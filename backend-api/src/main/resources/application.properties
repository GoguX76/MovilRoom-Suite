spring.application.name=backend-api

# --- CONEXIÓN A DOCKER ---
spring.datasource.url=jdbc:postgresql://localhost:5432/movilroom_db
spring.datasource.username=admin_scor
spring.datasource.password=admin_sc0r25
spring.datasource.driver-class-name=org.postgresql.Driver

# --- CONFIGURACIÓN JPA (Manejo de Tablas) ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Desactiva el control automático de Docker (Si es que ya se levanto Docker manualmente)
spring.docker.compose.enabled=false